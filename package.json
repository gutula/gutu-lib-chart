{
  "name": "gutu-lib-chart",
  "private": true,
  "type": "module",
  "workspaces": [
    "framework/libraries/*"
  ],
  "scripts": {
    "build": "bun run --cwd framework/libraries/chart build",
    "typecheck": "bun run --cwd framework/libraries/chart typecheck",
    "lint": "bun run --cwd framework/libraries/chart lint",
    "test": "bun run --cwd framework/libraries/chart test",
    "test:unit": "bun run --cwd framework/libraries/chart test:unit",
    "docs:summary": "node scripts/docs-summary.mjs",
    "docs:check": "node scripts/docs-check.mjs"
  }
}
